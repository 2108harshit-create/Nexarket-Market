✅ 1. Install GitHub CLI globally (`winget install --id GitHub.cli`) - running/pending PATH (restart VSCode if needed)


- [ ] 2. Authenticate GitHub CLI (`gh auth login`)
- [ ] 3. Create new feature branch (`git checkout -b blackboxai/update-changes`)
- [ ] 4. Stage all changes (`git add .`)
- [ ] 5. Commit changes (`git commit -m \"Update all recent changes: src/app features (buyer/seller/auth/products), lib/store/context, TODO progress\"`)
- [ ] 6. Push and create PR (`gh pr create --title \"Update all changes via BLACKBOXAI\" --body \"Automated update:\\n- Recent UI/page updates (open tabs)\\n- Store/context improvements\\n- TODO progress updates\"`)

**Progress will be updated after each step.**

Repo: https://github.com/2108harshit-create/Nexarket-Market
Current status: main ahead by 1 commit, TODO.md staged.
